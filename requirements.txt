# Runtime dependencies for the Vercel Python Function in api/.
# The quizpilot package itself is not installed; api/index.py adds src/ to
# sys.path, so this mirrors the runtime half of pyproject.toml plus the web extra.
fastapi>=0.115,<1
langchain>=1.4,<2
langchain-openrouter>=0.2.8,<0.3
langgraph>=1.0,<2
langgraph-checkpoint-postgres>=3.0,<4
psycopg[binary]>=3.2,<4
pydantic>=2.10,<3
python-dotenv>=1.0,<2
typing-extensions>=4.12,<5
